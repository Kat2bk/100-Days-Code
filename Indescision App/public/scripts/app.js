// JSX - JavaScript XML

var template = <p>This is JSX element</p>

var appRoot = document.getElementById(app);
ReactDOM.render(template, appRoot);
// takes two arguements, the JSX you want to render and the DOM element you want to render to